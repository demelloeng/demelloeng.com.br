# Public capture endpoint (Cloudflare Worker). NOT a secret - it is a public URL.
# Deployed in E5: integrations/cloudflare/README.md (engineering repo).
VITE_INTAKE_ENDPOINT=https://ma-demello-site-intake.ma-demello.workers.dev/api/site-intake

# PRÉVIA DEMELLO VERIFICÁVEL V1 — Worker ma-demello-preview (POST). URL pública, não é segredo.
VITE_PREVIEW_ENDPOINT=https://ma-demello-preview.ma-demello.workers.dev/api/previews/v1
