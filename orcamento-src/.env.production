# Public capture endpoint (Cloudflare Worker). NOT a secret - it is a public URL.
# Set to the real workers.dev (or intake.demelloeng.com.br) URL after E5 deploy:
#   integrations/cloudflare/README.md
VITE_INTAKE_ENDPOINT=https://site-intake.REPLACE-SUBDOMAIN.workers.dev/api/site-intake
