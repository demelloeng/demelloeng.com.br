# Local dev points at the offline Python harness:
#   python scripts/site_intake_http_ingest.py --port 8788   (engineering repo)
VITE_INTAKE_ENDPOINT=http://127.0.0.1:8788/api/site-intake

# Prévia verificável: por padrão sem endpoint local (o botão degrada para "no-endpoint").
# Para exercitar contra um Worker local: wrangler dev em integrations/cloudflare/preview-worker.
VITE_PREVIEW_ENDPOINT=
